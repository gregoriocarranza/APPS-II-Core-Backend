meta {
  name: UPDATE
  type: http
  seq: 4
}

put {
  url: {{core-BackendBackendHost}}/api/transfers/:uuid
  body: json
  auth: bearer
}

params:path {
  uuid: <uuid>
}

auth:bearer {
  token: {{JWT}}
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
}
