meta {
  name: UPDATE
  type: http
  seq: 3
}

put {
  url: {{core-BackendBackendHost}}/api/cursos/:uuid
  body: json
  auth: bearer
}

params:path {
  uuid: <uuid>
}

auth:bearer {
  token: {{core-BackendBearerToken}}
}

body:json {
  {}
}

settings {
  encodeUrl: true
}
