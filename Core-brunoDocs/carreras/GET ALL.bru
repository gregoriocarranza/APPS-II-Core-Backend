meta {
  name: GET ALL
  type: http
  seq: 1
}

get {
  url: {{core-BackendBackendHost}}/api/carreras
  body: none
  auth: bearer
}

params:query {
  ~name_carrera: tecnicatura de  Información
}

auth:bearer {
  token: {{JWT}}
}

settings {
  encodeUrl: true
  timeout: 0
}
