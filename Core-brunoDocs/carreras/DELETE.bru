meta {
  name: DELETE
  type: http
  seq: 5
}

delete {
  url: {{core-BackendBackendHost}}/api/carreras/:uuid
  body: none
  auth: bearer
}

params:path {
  uuid: <uuid>
}

auth:bearer {
  token: {{JWT}}
}

settings {
  encodeUrl: true
  timeout: 0
}
