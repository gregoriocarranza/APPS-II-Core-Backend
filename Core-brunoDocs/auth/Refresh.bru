meta {
  name: Refresh
  type: http
  seq: 2
}

post {
  url: {{core-BackendBackendHost}}/api/auth/refresh
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
  refreshtoken: 
}

auth:bearer {
  token: {{JWT}}
}

settings {
  encodeUrl: false
  timeout: 0
}
