meta {
  name: GET ALL
  type: http
  seq: 1
}

get {
  url: {{core-BackendBackendHost}}/api/wallets
  body: none
  auth: bearer
}

params:query {
  ~page: 1
  ~limit: 2
  ~user_uuid: 
}

auth:bearer {
  token: {{JWT}}
}

settings {
  encodeUrl: true
  timeout: 0
}
