meta {
  name: GET ALL
  type: http
  seq: 1
}

get {
  url: {{core-BackendBackendHost}}/api/notifications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{core-BackendBearerToken}}
}

settings {
  encodeUrl: true
}
